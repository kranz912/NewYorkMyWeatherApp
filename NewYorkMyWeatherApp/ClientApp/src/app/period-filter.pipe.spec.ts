import { PeriodFilterPipe } from './period-filter.pipe';

describe('PeriodFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PeriodFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
